apiVersion: v1
kind: PersistentVolume
metadata:
    name: products-database-volume
spec:
    storageClassName: manual
    capacity:
        storage: 500Mi
    accessModes:
        - ReadWriteOnce
    hostPath:
        path: '/mnt/data/products'
