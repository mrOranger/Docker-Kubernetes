apiVersion: v1
kind: Service
metadata:
    name: products-application-service
spec:
    type: LoadBalancer
    selector:
        deployment: products-application-deployment
    ports:
        - protocol: 'TCP'
          port: 80
          targetPort: 80
