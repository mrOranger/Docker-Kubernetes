apiVersion: v1
kind: Service
metadata:
    name: clients-application-service
spec:
    type: LoadBalancer
    selector:
        deployment: clients-application-deployment
    ports:
        - protocol: 'TCP'
          port: 80
          targetPort: 80
